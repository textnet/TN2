import { config } from "../config"


export function message( target, fullPayload ) {
}

export function connect( id:string, onMessage?, onConnect?, onClose? ) {
}

// onMessage(fromPeer, fullPayload)
// onClose(peer)
// onConnect(peer)